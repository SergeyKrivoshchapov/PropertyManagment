<UserControl x:Class="PropertyManagmentSystem.Views.BuildingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:enums="clr-namespace:PropertyManagmentSystem.Enums"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <ObjectDataProvider x:Key="FinishingTypes" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:FinishingType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Список зданий и форма добавления -->
        <StackPanel Grid.Column="0" Margin="6">
            <TextBlock Text="Здания" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
            <ListBox ItemsSource="{Binding Buildings}" SelectedItem="{Binding SelectedBuilding}" DisplayMemberPath="Address" Height="200"/>

            <GroupBox Header="Добавить здание" Margin="0,8,0,0">
                <StackPanel Margin="8">
                    <TextBox Text="{Binding NewBuildingDistrict, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />
                    <TextBox Text="{Binding NewBuildingAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Этажей:" VerticalAlignment="Center" Margin="0,6"/>
                        <TextBox Width="60" Text="{Binding NewBuildingFloorsCount, UpdateSourceTrigger=PropertyChanged}" Margin="6,4"/>
                        <TextBlock Text="Тел. коменданта:" VerticalAlignment="Center" Margin="12,6,0,6"/>
                        <TextBox Text="{Binding NewBuildingCommandantPhone, UpdateSourceTrigger=PropertyChanged}" Margin="6,4" Width="140"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8">
                        <Button Content="Добавить" Command="{Binding AddBuildingCommand}" Width="90" Margin="0,0,6,0"/>
                        <Button Content="Обновить" Command="{Binding UpdateBuildingCommand}" Width="90" Margin="0,0,6,0"/>
                        <Button Content="Удалить" Command="{Binding DeleteBuildingCommand}" Width="90"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <!-- Список комнат и форма -->
        <StackPanel Grid.Column="1" Margin="6">
            <TextBlock Text="Комнаты" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
            <ListBox ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom}" DisplayMemberPath="RoomNumber" Height="180"/>

            <GroupBox Header="Добавить комнату" Margin="0,8,0,0">
                <StackPanel Margin="8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="№:" VerticalAlignment="Center" Margin="0,6"/>
                        <TextBox Width="80" Text="{Binding NewRoomNumber, UpdateSourceTrigger=PropertyChanged}" Margin="6,4"/>
                        <TextBlock Text="Площадь:" VerticalAlignment="Center" Margin="12,6,0,6"/>
                        <TextBox Width="80" Text="{Binding NewRoomArea, UpdateSourceTrigger=PropertyChanged}" Margin="6,4"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,6">
                        <TextBlock Text="Этаж:" VerticalAlignment="Center" Margin="0,6"/>
                        <TextBox Width="80" Text="{Binding NewRoomFloor, UpdateSourceTrigger=PropertyChanged}" Margin="6,4"/>
                        <TextBlock Text="Отделка:" VerticalAlignment="Center" Margin="12,6,0,6"/>
                        <ComboBox Width="140" ItemsSource="{Binding Source={StaticResource FinishingTypes}}" SelectedItem="{Binding NewRoomFinishingType}" Margin="6,4"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,6">
                        <CheckBox IsChecked="{Binding NewRoomHasPhone}" Content="Есть телефон" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8">
                        <Button Content="Добавить" Command="{Binding AddRoomCommand}" Width="90" Margin="0,0,6,0"/>
                        <Button Content="Обновить" Command="{Binding UpdateRoomCommand}" Width="90" Margin="0,0,6,0"/>
                        <Button Content="Удалить" Command="{Binding DeleteRoomCommand}" Width="90"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Статистика" Margin="0,8,0,0">
                <StackPanel Margin="8">
                    <TextBlock Text="{Binding TotalRooms, StringFormat=Всего комнат: {0}}" Margin="0,2"/>
                    <TextBlock Text="{Binding AvailableRooms, StringFormat=Свободно: {0}}" Margin="0,2"/>
                    <TextBlock Text="{Binding OccupancyRate, StringFormat=Заполняемость: {0:P0}}" Margin="0,2"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>